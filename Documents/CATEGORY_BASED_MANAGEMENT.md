# 按分類管理選手和紀錄員

## 🎯 設計理念

在三層架構下，選手和隊伍都是**按 Category 報名**的，因此管理介面也應該**按 Category 分層**。

## 🔄 改進對比

### 舊版（已移除）

**選手管理 Tab**：

```
┌─────────────────────────────┐
│ 所有報名選手（混在一起）    │
│ - Alice (pending)           │
│ - Bob (confirmed)           │
│ - Carol/Dave (team)         │
│ - Eve (pending)             │
└─────────────────────────────┘
```

**問題**：

- 無法區分哪個選手報名哪個分類
- 單打和雙打混在一起
- 手動新增無法指定分類

### 新版（已實施）

**選手管理 Tab**：

```
┌─────────────────────────────────────┐
│ Tabs: [男子雙打 (18/20)] [女子單打 (12/16)] │
├─────────────────────────────────────┤
│ === 男子雙打 ===                    │
│                                     │
│ ┌─ Alice / Bob ──────────────┐    │
│ │ alice@email.com / bob@...   │    │
│ │ ✓ 已確認      [批准] [婉拒] │    │
│ └─────────────────────────────┘    │
│                                     │
│ ┌─ Carol / Dave ─────────────┐    │
│ │ carol@email.com / dave@...  │    │
│ │ 🕐 待審核      [批准] [婉拒] │    │
│ └─────────────────────────────┘    │
│                                     │
│ [+ 手動新增隊伍]                   │
└─────────────────────────────────────┘
```

**優勢**：

- 按分類分頁顯示
- 顯示報名進度（18/20）
- 單打顯示個人，雙打顯示隊伍
- 手動新增自動關聯到當前分類

## 📦 新增組件

### 1. CategoryPlayersManager.tsx

**功能**：

- 按 Category 分頁顯示報名者
- 單打顯示個人（Players）
- 雙打顯示隊伍（Teams，重疊頭像）
- 批准/婉拒功能
- 手動新增選手/隊伍

**核心邏輯**：

```typescript
// 1. 根據分類類型載入不同資料
if (currentCategory.matchType === "singles") {
  const playersData = await getPlayers(tournamentId);
  setPlayers(playersData);
} else {
  const teamsData = await getTeamsByCategory(tournamentId, activeCategory);
  setTeams(teamsData);
}

// 2. 根據分類類型執行不同操作
if (currentCategory.matchType === "singles") {
  await approvePlayer(tournamentId, playerId);
} else {
  await approveTeam(tournamentId, categoryId, teamId);
}
```

### 2. CategoryStaffManager.tsx

**功能**：

- 按 Category 分頁顯示（便於查看分配情況）
- 邀請紀錄員（全局，不綁定分類）
- 移除邀請
- 顯示狀態（邀請中/已接受/已拒絕）

**特殊說明**：

```
提示：紀錄員不需要按分類邀請，他們可以記錄所有分類的比賽。
以下按分類顯示，方便查看各分類的紀錄員分配情況。
```

**設計考量**：

- 紀錄員是全局資源（不綁定特定分類）
- 按分類分頁主要是為了 UI 組織清晰
- 未來可以按分類分配紀錄員（進階功能）

## 🎨 UI 設計

### 選手管理 Tab

#### 單打分類

```
┌─────── 女子單打 (12/16) ───────┐
│                                │
│ ┌─ Alice ─────────────────┐   │
│ │ 👤                       │   │
│ │ Alice Wang               │   │
│ │ alice@email.com          │   │
│ │ ✓ 已確認  手動新增       │   │
│ └──────────────────────────┘   │
│                                │
│ ┌─ Bob ───────────────────┐   │
│ │ 👤                       │   │
│ │ Bob Chen                 │   │
│ │ bob@email.com            │   │
│ │ 🕐 待審核  [批准] [婉拒]  │   │
│ └──────────────────────────┘   │
│                                │
│ [+ 手動新增選手]               │
└────────────────────────────────┘
```

#### 雙打分類

```
┌─────── 男子雙打 (18/20) ───────┐
│                                │
│ ┌─ Team Alpha ────────────┐   │
│ │ 👤👤 (重疊頭像)         │   │
│ │ Alice / Bob              │   │
│ │ alice@... / bob@...      │   │
│ │ ✓ 已確認                 │   │
│ └──────────────────────────┘   │
│                                │
│ ┌─ Team Beta ─────────────┐   │
│ │ 👤👤                     │   │
│ │ Carol / Dave             │   │
│ │ carol@... / dave@...     │   │
│ │ 🕐 待審核  [批准] [婉拒]  │   │
│ └──────────────────────────┘   │
│                                │
│ [+ 手動新增隊伍]               │
└────────────────────────────────┘
```

### 手動新增 Modal

#### 單打

```
┌────── 手動新增選手 - 女子單打 ──────┐
│                                      │
│ 選手 Email:                          │
│ ┌────────────────┐ [搜尋]           │
│ └────────────────┘                  │
│                                      │
│ ✓ 找到用戶：Alice Wang              │
│                                      │
│ 選手姓名:                            │
│ ┌────────────────┐                  │
│ │ Alice Wang     │                  │
│ └────────────────┘                  │
│                                      │
│                  [取消]  [新增]     │
└──────────────────────────────────────┘
```

#### 雙打

```
┌────── 手動新增隊伍 - 男子雙打 ──────┐
│                                      │
│ 選手1 Email:                         │
│ ┌────────────────┐ [搜尋]           │
│ └────────────────┘                  │
│                                      │
│ 選手1 姓名:                          │
│ ┌────────────────┐                  │
│ │ Alice Wang     │                  │
│ └────────────────┘                  │
│                                      │
│ ─────── 雙打隊友 ───────             │
│                                      │
│ 選手2 Email (選填):                  │
│ ┌────────────────┐                  │
│ └────────────────┘                  │
│                                      │
│ 選手2 姓名:                          │
│ ┌────────────────┐                  │
│ │ Bob Chen       │                  │
│ └────────────────┘                  │
│                                      │
│                  [取消]  [新增]     │
└──────────────────────────────────────┘
```

### 紀錄員管理 Tab

```
┌──────────────────────────────────────┐
│ 提示：紀錄員不需要按分類邀請...  │
├──────────────────────────────────────┤
│ Tabs: [男子雙打] [女子單打]         │
├──────────────────────────────────────┤
│ === 男子雙打 ===                     │
│                                      │
│ 紀錄員列表                           │
│ 目前共 3 位已接受邀請的紀錄員       │
│                                      │
│ ┌─ John Doe ──────────────────┐    │
│ │ 👤                           │    │
│ │ John Doe                     │    │
│ │ john@email.com               │    │
│ │ 紀錄員  ✓ 已接受             │    │
│ └──────────────────────────────┘    │
│                                      │
│ ┌─ Jane Smith ────────────────┐    │
│ │ 👤                           │    │
│ │ Jane Smith                   │    │
│ │ jane@email.com               │    │
│ │ 紀錄員  🕐 邀請中  [取消邀請] │    │
│ └──────────────────────────────┘    │
│                                      │
│ [+ 邀請紀錄員]                      │
└──────────────────────────────────────┘
```

## 🔧 技術實現

### 組件結構

```
TournamentDashboard
├── Tab: 賽事資訊
├── Tab: 選手管理 ⭐ 重構
│   └── CategoryPlayersManager
│       ├── Category Tabs
│       ├── Players/Teams List
│       └── Add Player/Team Modal
├── Tab: 紀錄員管理 ⭐ 重構
│   └── CategoryStaffManager
│       ├── Category Tabs
│       ├── Staff List
│       └── Invite Staff Modal
└── Tab: 賽程管理
```

### 數據流

#### 選手管理

```typescript
// 載入資料
const loadParticipants = async () => {
  if (currentCategory.matchType === "singles") {
    const playersData = await getPlayers(tournamentId);
    setPlayers(playersData);
  } else {
    const teamsData = await getTeamsByCategory(tournamentId, activeCategory);
    setTeams(teamsData);
  }
};

// 手動新增
const handleAdd = async () => {
  if (currentCategory.matchType === "singles") {
    await addPlayerManually(tournamentId, playerData);
  } else {
    await createTeam(tournamentId, activeCategory, teamData);
  }
};

// 批准/婉拒
if (isSingles) {
  await approvePlayer(tournamentId, playerId);
} else {
  await approveTeam(tournamentId, categoryId, teamId);
}
```

#### 紀錄員管理

```typescript
// 載入紀錄員（全局）
const data = await getStaff(tournamentId);
setStaff(data);

// 邀請紀錄員（全局）
await inviteStaff(tournamentId, staffData);

// 移除邀請
await removeStaff(tournamentId, staffId);
```

## 📊 數據對應

### 單打報名

```
Tournament
└── Categories
    └── Category: "女子單打"
        └── (暫存在 tournaments/{id}/players)
            ├── Player 1 (Alice)
            ├── Player 2 (Bob)
            └── Player 3 (Carol)
```

**未來改進**：

- 可以遷移到 `tournaments/{id}/categories/{catId}/registrations`

### 雙打報名

```
Tournament
└── Categories
    └── Category: "男子雙打"
        └── teams/{teamId}
            ├── Team 1 (Alice / Bob)
            ├── Team 2 (Carol / Dave)
            └── Team 3 (Eve / Frank)
```

### 紀錄員

```
Tournament
└── staff/{staffId}
    ├── Staff 1 (John)
    ├── Staff 2 (Jane)
    └── Staff 3 (Jake)
```

**說明**：紀錄員是全局的，不綁定特定分類

## 🎨 交互設計

### Category Tabs

**顯示格式**：

```typescript
{
  id: "cat-001",
  label: "男子雙打 (18/20)"  // 名稱 + 報名進度
}
```

**切換行為**：

- 點擊 Tab → 載入該分類的參賽者
- 單打/雙打自動切換顯示格式
- 手動新增 Modal 標題顯示當前分類

### 批准/婉拒按鈕

**顯示條件**：

- 僅 `status === "pending"` 時顯示
- 批准中/婉拒中顯示 loading 狀態
- 操作中禁用其他按鈕

**操作後**：

- 重新載入參賽者列表
- 更新 UI 顯示

## 🚀 使用流程

### 主辦方：審核報名

1. **進入控制台**

   ```
   導航：主辦方首頁 → 點擊賽事 → 選手管理 Tab
   ```

2. **切換分類**

   ```
   點擊：[男子雙打] Tab
   ```

3. **查看報名列表**

   ```
   顯示：所有男子雙打的報名隊伍
   狀態：待審核/已確認
   ```

4. **批准/婉拒**
   ```
   點擊：[批准] 或 [婉拒] 按鈕
   結果：狀態更新，通知發送給選手
   ```

### 主辦方：手動新增

#### 新增單打選手

1. 切換到「女子單打」Tab
2. 點擊「手動新增選手」
3. 輸入 Email → 點擊「搜尋」
   - 找到：自動填入姓名和頭像
   - 沒找到：創建影子帳號
4. 確認姓名
5. 點擊「新增」

#### 新增雙打隊伍

1. 切換到「男子雙打」Tab
2. 點擊「手動新增隊伍」
3. 輸入選手 1 資訊
   - Email（可選）
   - 姓名（必填）
4. 輸入選手 2 資訊
   - Email（可選）
   - 姓名（必填）
5. 點擊「新增」
6. 隊伍創建，狀態為「已確認」

## 📋 修改清單

### 新增文件（4 個）

- `src/components/features/CategoryPlayersManager.tsx`
- `src/components/features/CategoryPlayersManager.module.scss`
- `src/components/features/CategoryStaffManager.tsx`
- `src/components/features/CategoryStaffManager.module.scss`

### 修改文件（1 個）

- `src/pages/organizer/TournamentDashboard.tsx`
- 導入 CategoryPlayersManager 和 CategoryStaffManager
- 載入 categories
- 替換選手管理 Tab 內容
- 替換紀錄員管理 Tab 內容

### 移除代碼

- 舊的選手列表顯示邏輯（~80 行）
- 舊的紀錄員列表顯示邏輯（~70 行）
- 手動新增選手的 Modal（~100 行）
- 邀請紀錄員的 Modal（~50 行）

**淨效果**：程式碼更簡潔，功能更強大

## 🎯 功能對照表

| 功能             | 舊版 | 新版 |
| ---------------- | ---- | ---- |
| 顯示報名選手     |      |      |
| 按分類顯示       |      | ⭐   |
| 單打/雙打區分    |      | ⭐   |
| 顯示報名進度     |      | ⭐   |
| 批准/婉拒        |      |      |
| 手動新增選手     |      |      |
| 手動新增雙打隊伍 |      | ⭐   |
| Email 搜尋用戶   |      |      |
| 影子帳號         |      |      |
| 邀請紀錄員       |      |      |
| 按分類查看紀錄員 |      | ⭐   |

## 🔗 相關 Services

### 使用的 Service 函數

**registrationService.ts**：

- `getPlayers(tournamentId)` - 獲取選手
- `addPlayerManually(tournamentId, playerData)` - 手動新增選手
- `approvePlayer(tournamentId, playerId)` - 批准選手
- `rejectPlayer(tournamentId, playerId)` - 婉拒選手

**teamService.ts**：

- `getTeamsByCategory(tournamentId, categoryId)` - 獲取隊伍
- `createTeam(tournamentId, categoryId, teamData)` - 新增隊伍
- `approveTeam(tournamentId, categoryId, teamId)` - 批准隊伍
- `rejectTeam(tournamentId, categoryId, teamId)` - 婉拒隊伍

**staffService.ts**：

- `getStaff(tournamentId)` - 獲取紀錄員
- `inviteStaff(tournamentId, staffData)` - 邀請紀錄員
- `removeStaff(tournamentId, staffId)` - 移除紀錄員

## 測試場景

### 場景 1：審核單打報名

1. 進入選手管理 Tab
2. 切換到「女子單打」
3. 看到 5 個待審核選手
4. 批准 Alice
5. 婉拒 Bob
6. 列表更新，Alice 顯示「已確認」

### 場景 2：手動新增雙打隊伍

1. 進入選手管理 Tab
2. 切換到「男子雙打」
3. 點擊「手動新增隊伍」
4. 輸入 alice@email.com → 搜尋 → 找到用戶
5. 輸入 bob@email.com → 搜尋 → 沒找到
6. 手動輸入 Bob 的姓名
7. 點擊「新增」
8. 隊伍出現在列表中

### 場景 3：邀請紀錄員

1. 進入紀錄員管理 Tab
2. 點擊「邀請紀錄員」
3. 輸入 john@email.com 和 John Doe
4. 點擊「發送邀請」
5. John 出現在列表中，狀態「邀請中」

### 場景 4：按分類查看統計

1. 進入選手管理 Tab
2. 切換不同 Tab：
   - 男子雙打 (18/20) - 18 支隊伍
   - 女子單打 (12/16) - 12 位選手
   - 混合雙打 (10/24) - 10 支隊伍
3. 清晰查看各分類報名情況

## 🔮 未來擴展

### 短期（建議）

- [ ] 按分類分配紀錄員
- [ ] 顯示各分類的紀錄員數量
- [ ] 紀錄員偏好設定（可記錄哪些分類）

### 中期

- [ ] 批量審核（全選/批量批准）
- [ ] 導出報名名單（按分類）
- [ ] 報名狀態篩選（待審核/已確認/已拒絕）

### 長期

- [ ] 紀錄員工作量統計
- [ ] 選手/隊伍排序（按報名時間/姓名）
- [ ] 報名資料編輯功能

## 📝 注意事項

### 單打 vs 雙打

- **單打**：儲存在 `players` 集合（暫時，未來可能遷移）
- **雙打**：儲存在 `categories/{catId}/teams` 集合

### 權限控制

- 只有主辦方可以批准/婉拒
- 只有主辦方可以手動新增
- 只有主辦方可以邀請紀錄員

### 數據同步

- 批准/婉拒後自動重新載入列表
- 新增後自動重新載入列表
- 使用當前分類的最新資料

---

**實施日期**: 2024 年 12 月 21 日  
**狀態**: 已完成  
**影響**: 選手管理和紀錄員管理更清晰、更易用
