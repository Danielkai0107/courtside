# 🧪 測試數據生成器

## 功能說明

為了方便測試三層賽事架構，在選手管理頁面添加了**測試數據生成器**，可以快速批量生成測試選手或隊伍。

## 🎯 使用位置

```
主辦方控制台
  → 選手管理 Tab
    → [手動新增選手/隊伍] [🧪 測試數據] ← 這裡
```

## 📱 使用流程

### 1. 開啟測試數據生成器

1. 進入「賽事控制台」→「選手管理」Tab
2. 切換到想要測試的分類（例如：男子雙打）
3. 點擊「🧪 測試數據」按鈕

### 2. 設定生成數量

```
┌────── 生成測試數據 - 男子雙打 ──────┐
│                                      │
│ 此功能僅供測試使用                │
│ 將自動生成指定數量的測試隊伍，      │
│ 所有狀態為「已確認」。              │
│                                      │
│ 生成數量（隊伍）:                    │
│ ┌────────────┐                      │
│ │    10      │                      │
│ └────────────┘                      │
│ 提示：1-50                           │
│                                      │
│            [取消]  [生成測試數據]   │
└──────────────────────────────────────┘
```

### 3. 確認生成

點擊「生成測試數據」→ 系統開始批量創建

### 4. 完成

```
 成功新增 10 個測試隊伍！
```

列表自動更新，顯示所有測試數據。

## 🎨 生成規則

### 單打選手

**命名規則**：

```
測試選手 Alice 1
測試選手 Bob 2
測試選手 Carol 3
...
```

**Email 規則**：

```
test{timestamp}-0@example.com
test{timestamp}-1@example.com
test{timestamp}-2@example.com
...
```

**狀態**：

- 自動設為「已確認」（confirmed）
- 標記為「手動新增」（manualAdded: true）

### 雙打隊伍

**命名規則**：

```
測試選手 Alice 1 / 測試選手 Bob 1
測試選手 Carol 2 / 測試選手 Dave 2
測試選手 Eve 3 / 測試選手 Frank 3
...
```

**Email 規則**：

```
Player 1: test{timestamp}-0-1@example.com
Player 2: test{timestamp}-0-2@example.com
...
```

**狀態**：

- 自動設為「已確認」（confirmed）
- 兩位選手都是影子帳號

## 測試場景

### 場景 1：測試小組賽分組

```
目標：測試 20 隊的小組賽分組

1. 創建賽事，設定「男子雙打」分類（名額 20）
2. 進入選手管理 → 切換到「男子雙打」
3. 點擊「🧪 測試數據」
4. 輸入數量：20
5. 點擊「生成測試數據」
6.  20 支隊伍立即生成
7. 進入賽程設定
8. 系統推薦：4 組循環賽 → 8 強淘汰
9. 發布賽程
10.  測試分組邏輯
```

### 場景 2：測試不規則人數

```
目標：測試 13 人的智能推薦

1. 設定「女子單打」分類（名額 16）
2. 生成 13 個測試選手
3.  系統推薦：
   - 方案 A：4 組（3,3,3,4 隊）
   - 方案 B：3 組（4,4,5 隊）
```

### 場景 3：測試自動晉級

```
目標：測試淘汰賽自動晉級

1. 生成 8 個測試選手
2. 發布純淘汰賽程
3. 紀錄員記錄比賽結果
4.  勝者自動晉級到下一場
5.  下一場自動顯示晉級選手
```

### 場景 4：測試 BYE 輪空

```
目標：測試輪空機制

1. 生成 13 個測試選手
2. 發布純淘汰賽程（系統補到 16 強）
3.  3 個 BYE 場次自動完成
4.  輪空選手自動晉級第二輪
```

## 注意事項

### 限制

1. **數量限制**：1-50 之間

   - 避免生成過多數據影響性能
   - 測試夠用即可

2. **Email 唯一性**：使用時間戳確保不重複

   ```typescript
   const timestamp = Date.now();
   email: `test${timestamp}-${i}@example.com`;
   ```

3. **名字循環**：使用 50 個預設名字循環
   ```typescript
   const names = ["Alice", "Bob", "Carol", ...];
   name: names[i % names.length]
   ```

### 清理測試數據

**方法 1：重新創建賽事**

- 刪除舊賽事，創建新的

**方法 2：手動刪除**（未來功能）

- 可以添加批量刪除功能

**方法 3：使用新賽事**

- 每次測試創建新賽事

## 🔧 技術實現

### 核心代碼

```typescript
const handleGenerateTestData = async () => {
  const count = parseInt(testCount) || 10;
  const timestamp = Date.now();

  for (let i = 0; i < count; i++) {
    if (currentCategory.matchType === "singles") {
      // 生成單打選手
      await addPlayerManually(tournamentId, {
        email: `test${timestamp}-${i}@example.com`,
        name: `測試選手 ${names[i % names.length]} ${i + 1}`,
      });
    } else {
      // 生成雙打隊伍
      await createTeam(tournamentId, activeCategory, {
        player1Id: `shadow-${timestamp}-${i}-1`,
        player2Id: `shadow-${timestamp}-${i}-2`,
        player1Name: `測試選手 ${names[i]} ${i + 1}`,
        player2Name: `測試選手 ${names[i + 1]} ${i + 1}`,
        player1Email: `test${timestamp}-${i}-1@example.com`,
        player2Email: `test${timestamp}-${i}-2@example.com`,
      });
    }
  }
};
```

### 數據存儲

**單打**：

```
tournaments/{tournamentId}/players/{playerId}
```

**雙打**：

```
tournaments/{tournamentId}/categories/{categoryId}/teams/{teamId}
```

## 📊 生成速度

| 數量 | 類型 | 預計時間  |
| ---- | ---- | --------- |
| 10   | 單打 | ~2-3 秒   |
| 10   | 雙打 | ~3-5 秒   |
| 20   | 單打 | ~5-7 秒   |
| 20   | 雙打 | ~7-10 秒  |
| 50   | 單打 | ~15-20 秒 |
| 50   | 雙打 | ~20-30 秒 |

## 功能檢查清單

- [x] 測試按鈕顯示在正確位置
- [x] 點擊開啟測試 Modal
- [x] 可以設定生成數量（1-50）
- [x] 顯示警告訊息
- [x] 單打生成正確
- [x] 雙打生成正確
- [x] 生成後自動刷新列表
- [x] 顯示成功訊息
- [x] Loading 狀態正確
- [x] 錯誤處理正確

## 🎯 測試建議

### 快速測試流程

1. **創建測試賽事**

   ```
   名稱：2025 測試錦標賽
   分類1：男子雙打（20 組）- 小組+淘汰
   分類2：女子單打（16 人）- 純淘汰
   ```

2. **生成測試數據**

   ```
   男子雙打：生成 20 支隊伍
   女子單打：生成 16 位選手
   ```

3. **測試賽程生成**

   ```
   男子雙打：
     - 查看智能推薦
     - 選擇方案 A
     - 發布賽程

   女子單打：
     - 直接發布純淘汰賽程
   ```

4. **測試比賽流程**

   ```
   - 紀錄員記分
   - 驗證自動晉級
   - 驗證場地調度
   ```

5. **測試完成**
   ```
   - 標記賽事完成
   - 查看冠亞季軍
   ```

## 🔗 相關功能

- **選手管理**: `CategoryPlayersManager.tsx`
- **手動新增**: 同一組件中的另一個 Modal
- **批准/婉拒**: 審核測試選手

## 開發技巧

### 快速填滿分類

```bash
# 快速生成滿額測試數據
男子雙打 (20 組) → 生成 20
女子單打 (16 人) → 生成 16
混合雙打 (24 組) → 生成 24
```

### 測試各種人數

```bash
# 測試智能推薦的不同場景
13 人 → 3-4 組方案
17 人 → 4-5 組方案
25 人 → 5-6 組方案
```

### 測試極限情況

```bash
# 最少人數
2 人 → 純淘汰（決賽）
4 人 → 純淘汰（準決賽+決賽）

# 不規則人數
13, 17, 21, 25... → 測試分組平衡
```

---

## 🎉 完成

**測試數據生成器已添加！**

現在您可以：

- 快速生成測試選手/隊伍
- 測試智能分組推薦
- 測試賽程生成邏輯
- 測試自動晉級機制
- 大幅提升開發測試效率

**使用提示**：

- 🧪 僅供測試環境使用
- 📊 建議生成 10-20 個測試數據
- 🗑️ 測試完畢可刪除賽事重新創建

---

**實施日期**: 2024 年 12 月 21 日  
**位置**: CategoryPlayersManager 組件  
**狀態**: 可用
