# 導航路徑優化

## 🎯 問題

部分頁面的返回按鈕會跳回首頁，打斷用戶操作流程。

## 修復的路徑

### 紀錄員路徑

#### ScoringConsole（計分頁面）

**修復前**：

```
計分頁面 → 點擊返回 → /scorer（首頁）❌
```

**修復後**：

```
計分頁面 → 點擊返回 → navigate(-1)（上一頁）
→ 返回到 ScorerCategoryDetail（分類詳情）
```

**流程**：

```
ScorerHome
  ↓
TournamentMatches（類別列表）
  ↓
ScorerCategoryDetail（分類詳情）
  ↓
ScoringConsole（計分）
  ↓ 點擊返回
ScorerCategoryDetail  正確
```

### 主辦方路徑

#### TournamentDashboard

**保持**：

```
控制台 → 返回 → /organizer（主辦方首頁）
```

#### CreateTournament / EditTournament

**保持**：

```
創建/編輯 → 完成 → /organizer/tournaments/:id（控制台）
創建/編輯 → 返回 → navigate(-1)（上一頁）
```

### 選手路徑

#### EventDetail / CategoryDetail

**保持**：

```
賽事詳情 → 返回 → navigate(-1)（上一頁）
分類詳情 → 返回 → navigate(-1)（上一頁）
```

## 📊 完整導航地圖

### 紀錄員流程（修復後）

```
/scorer（首頁）
  ├─ Tabs: [正在進行] [過去記錄]
  └─ 賽事卡片
      ↓ 點擊
/scorer/tournaments/:id（類別列表）
  └─ 類別卡片
      ↓ 點擊
/scorer/tournaments/:id/categories/:categoryId（分類詳情）
  ├─ Tabs: [小組] [對陣圖] [球員]
  └─ 比賽列表
      ↓ 點擊比賽
/scorer/matches/:id（計分頁面）
  ↓ 點擊返回（navigate(-1)）
返回 ScorerCategoryDetail
```

### 主辦方流程

```
/organizer（首頁）
  ├─ Tabs: [進行中] [歷史記錄]
  └─ 賽事卡片
      ↓ 點擊
/organizer/tournaments/:id（控制台）
  ├─ Tabs: [資訊] [選手] [紀錄員] [場地] [賽程]
  ├─ 點擊「編輯」→ /organizer/tournaments/:id/edit
  │   ↓ 完成 → 返回控制台
  └─ 點擊返回 → /organizer（首頁）
```

### 選手流程

```
/events（賽事列表）
  └─ 賽事卡片
      ↓ 點擊
/events/:id（賽事詳情）
  ├─ Tabs: [類別] [圖庫] [結果] [信息]
  └─ 類別卡片
      ↓ 點擊
/events/:id/categories/:categoryId（分類詳情）
  ├─ Tabs: [小組] [對陣圖] [球員]
  └─ 點擊返回 → navigate(-1)
      → 返回 EventDetail
```

## 📋 修改清單

### 修改文件（3 個）

- `src/pages/scorer/ScoringConsole.tsx`
- 返回按鈕：`navigate("/scorer")` → `navigate(-1)`
- 完成比賽後：`navigate("/scorer")` → `navigate(-1)`
- 錯誤頁面：`navigate("/scorer")` → `navigate(-1)`

- `src/pages/organizer/EditTournament.tsx`
- 添加註釋說明返回邏輯

- `src/pages/organizer/CreateTournament.tsx`
- 添加註釋說明返回邏輯

### 保持不變（正確的）

- `CategoryDetail.tsx` - `navigate(-1)`
- `EventDetail.tsx` - `navigate(-1)`
- `TournamentDashboard.tsx` - `navigate("/organizer")`
- `TournamentMatches.tsx` - `navigate("/scorer")`
- `ScorerCategoryDetail.tsx` - `navigate(\`/scorer/tournaments/${id}\`)`

## 🎯 返回邏輯原則

### 使用 `navigate(-1)`（瀏覽器返回）

**適用**：

- 詳情頁面（EventDetail, CategoryDetail）
- 編輯頁面（返回按鈕）
- 計分頁面（ScoringConsole）⭐ 修復

**優點**：

- 保持用戶的瀏覽歷史
- 返回到實際的上一頁
- 符合用戶預期

### 使用明確路徑

**適用**：

- 完成操作後（創建/編輯完成）
- 錯誤頁面（導航到安全位置）
- 頂層頁面的返回（控制台 → 首頁）

**優點**：

- 確保到達正確位置
- 避免返回到錯誤狀態

## 測試檢查清單

### 紀錄員流程

- [x] 首頁 → 賽事 → 類別 → 計分 → 返回 → 回到類別詳情
- [x] 計分完成 → 返回 → 回到類別詳情
- [x] 類別詳情 → 返回 → 回到類別列表
- [x] 類別列表 → 返回 → 回到首頁

### 主辦方流程

- [x] 首頁 → 控制台 → 返回 → 回到首頁
- [x] 控制台 → 編輯 → 完成 → 回到控制台
- [x] 控制台 → 編輯 → 返回 → 回到控制台

### 選手流程

- [x] 賽事列表 → 賽事詳情 → 返回 → 回到列表
- [x] 賽事詳情 → 分類詳情 → 返回 → 回到賽事詳情

## 🎊 完成

**所有返回路徑已優化！**

- 紀錄員計分頁面返回正確
- 保持用戶操作流程
- 不會意外跳回首頁
- 符合用戶預期

**導航體驗更流暢！** 🚀

---

**修復日期**: 2024 年 12 月 21 日  
**狀態**: 已完成  
**影響**: 用戶體驗提升
